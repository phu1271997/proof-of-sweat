# Address of the deployed Proof of Sweat contract on GenLayer studionet.
# Deploy contracts/proof_of_sweat.py in GenLayer Studio, copy the address here,
# then restart the dev server (or redeploy). See ../README.md.
VITE_CONTRACT_ADDRESS=0xYourDeployedContractAddressHere
