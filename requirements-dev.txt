# Contract tests (direct mode, in-memory — no Docker, no network).
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-dev.txt
#   pytest tests/ -v
genlayer-test[sim]
