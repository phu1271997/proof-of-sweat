# genlayer-test's direct-mode fixtures (direct_vm, direct_deploy, direct_alice, ...)
# are auto-registered by the installed plugin, so no explicit registration is needed.
# Run with:  pytest tests/ -v   (after: pip install "genlayer-test[sim]")
